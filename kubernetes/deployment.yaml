apiVersion: apps/v1
kind: Deployment
metadata:
  name: tigergate-test-go
spec:
  replicas: 1
  selector:
    matchLabels: { app: tigergate-test-go }
  template:
    metadata:
      labels: { app: tigergate-test-go }
    spec:
      containers:
        - name: app
          image: tigergate-test-go:latest
